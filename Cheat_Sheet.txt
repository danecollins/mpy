Python Cheat Sheet

Define a function
	def getAcctNumber(map,x):



Properly stripping newlines
	s = s.rstrip(‘\r\n’)


Boolean Values
	USConsolidated = True

Strings
	x = x.replace('?','') -- replacing a character
	dept = x[0:3]         -- substring
	x.find('subst') <> -1 -- subst in x
	x.strip()             -- remove leading and trailing spaces
	
String Formatting
	str = "loc is {lat} {lon}"
	pos = {'lat': "34" , 'lon': "45"}
	str.format(**pos)

')

Rounding - convert to number
	balance = round(float(balance),2)

===== LISTS =====
	l=[]          -- initialize an empty list
	[a,b,c]       -- build a list
	l.append(foo) -- append to a list
	(a,b) = l     -- split a list
	len(l)		  -- length
	l.insert(pos,item) -- inserts into middle of list
	l.index(item) -- returns position of item, error if missing
	l.sort()	  -- in place sort
	l.remove(item)

Terminating a loop iteration
	next   - do next iteration
	break  - exit loop early

If
	elif not (initialized) - odd elif syntax and not instead of !


Looping through 2 lists at once
	for name,value in zip(header_lines,bias_data):



===== FILE =====	  
Print to a file
	print >> of, 'O,%s,%s,%s,%s,%s,%s,,%s,%s' % (Co,ni_cc,'61096',Sub,PL,Proj,2,'Other benefits
	
Open a file
	file=open(sys.argv[1])
	of = open('output.csv','w')
	file.close()
	file.readlines() returns a list of lines
	file.read() reads entire file into a string
	
Iterating files in a directory
	import glob
	spar_files = glob.glob('*.s2p')
	for f in spar_files:
		print f

Looping through a file
	for line in file.readlines():
	
===== DICTIONARY =====
	foo = {'a':1, 'b':2} to init
	del dict_name[key_name]
	
	
Catching error accessing value in array	
	try:
	  thevariable
	except NameError:
	  print "well, it WASN'T defined after all!"
	else:
	  print "sure, it was defined."